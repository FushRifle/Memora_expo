const { withTamaguiMetro } = require('tamagui/metro-config')

module.exports = withTamaguiMetro({
    // your existing Expo config here if needed
    alias: {
        // optional: custom path aliases
    },
})
